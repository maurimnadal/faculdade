// Página pública que descreve o propósito do sistema.
export default function About() {
    return (
        <section className="card">
            <h1>About</h1>
            <p>Exemplo didático de integração React + API Node/Express com JWT.</p>
            <p>Recursos: rotas públicas/privadas, guarda por papel e consumo de
                API.</p>
        </section>
    );
}
